version: '3.5'

services:

  jaeger:
    container_name: jaeger
    image: jaegertracing/all-in-one:latest
    hostname: jaeger
    ports:
      - 5778:5778
      - 16686:16686
      - 14268:14268
    networks:
    - spring-rest-router

networks:
  spring-rest-router:
    driver: bridge
